# ── 复制本文件为 .env 后填写；`npm run dev` / `npm start` 会自动加载 ──
# .env 已被 .gitignore 忽略，切勿提交真实密钥。

# ===== LLM（可选，不填回退本地 Ollama）=====
# LLM_PROVIDER=deepseek
# DEEPSEEK_API_KEY=sk-xxx
# DEEPSEEK_MODEL=deepseek-v4-flash

# ===== LangSmith Tracing（可选）=====
# 打开后，/api/agent/stream 与 /api/agent/run 会把 Agent、工具、LLM 调用写入 LangSmith。
# 真实 API Key 只写本地 .env，不要提交。
# LANGSMITH_TRACING=true
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LANGSMITH_API_KEY=<your-langsmith-api-key>
# LANGSMITH_PROJECT=verb

# ===== 服务监听 =====
# 本地开发默认监听 127.0.0.1；部署到容器/局域网时可设为 0.0.0.0。
# HOST=127.0.0.1
# PORT=3456

# ===== 用户认证（生产必设；不设则用不安全的开发默认值）=====
# AUTH_SECRET=换成一段随机长字符串

# ===== 持久化用户数据（推荐 Supabase / Neon Postgres）=====
# 配置后，账号、练习记录、SRS 卡片、Agent 长期记忆、订单和权益写入 Postgres。
# 词典、FTS 与 sqlite-vec 知识库仍使用构建产出的本地 SQLite。
# Supabase 建议复制 Transaction pooler 连接串（端口 6543），并保留 sslmode=require。
# DATABASE_URL=postgresql://postgres.<project-ref>:<password>@<host>:6543/postgres?sslmode=require
# DATABASE_SSL=true
# DATABASE_POOL_SIZE=5

# ===== 支付宝支付（沙箱）=====
# 不填这三项 → 自动用 mock provider（零资金演示，无需任何资质）。
# 填齐 → 自动切真实支付宝。沙箱密钥获取：
#   开放平台控制台 → 研发服务 → 沙箱 → 「沙箱应用」拿 APPID；
#   用「支付宝密钥工具」生成应用私钥/公钥，上传应用公钥后页面给出「支付宝公钥」。
#
# APPID（沙箱应用的，不是你正式应用的）
# ALIPAY_APP_ID=
# 应用私钥（密钥工具生成的"应用私钥"，整段一行，去掉 BEGIN/END 和换行）
# ALIPAY_PRIVATE_KEY=
# 支付宝公钥（上传应用公钥后平台返回的那串，不是你自己的应用公钥！）
# ALIPAY_PUBLIC_KEY=
#
# 沙箱基址。生产环境删掉这行走默认。endpoint 给 v3，网页支付的表单网关会自动
# 推导为 <endpoint>/gateway.do（也可用 ALIPAY_GATEWAY 单独覆盖）。
# ALIPAY_ENDPOINT=https://openapi-sandbox.dl.alipaydev.com
#
# 私钥格式：密钥工具默认 PKCS8；若验签报错可改成 PKCS1
# ALIPAY_KEY_TYPE=PKCS8
#
# 支付方式：page=电脑网站支付（浏览器收银台，无需 App，默认）；qr=当面付扫码（需 App 扫）
# ALIPAY_PAY_MODE=page
# 付款完成后浏览器跳回地址（可选，仅 UX；到账以轮询为准）
# ALIPAY_RETURN_URL=http://localhost:5173

# ===== 飞书机器人入口（可选）=====
# 飞书开放平台 → 应用凭证
# FEISHU_APP_ID=cli_xxx
# FEISHU_APP_SECRET=xxx
# FEISHU_DOMAIN=feishu
#
# 入口模式：
# - websocket：长连接，本机主动连接飞书，不需要公网映射。需要安装 @larksuiteoapi/node-sdk。
# - webhook：HTTP 回调，需要飞书能访问你的公网 HTTPS 地址。
# - disabled：不启用飞书入口。
# FEISHU_CONNECTION_MODE=disabled
#
# 飞书开放平台 → 事件订阅 → Verification Token
# webhook 模式必填；websocket 模式可填，用于 SDK 事件校验/解密配置。
# FEISHU_VERIFICATION_TOKEN=xxx
# FEISHU_ENCRYPT_KEY=xxx
